# API base URL used by src/api.ts.
# Dev: leave unset → '' → relative '/api/*' goes through the Vite proxy (vite.config.ts) to localhost:8000.
# Prod: set to the deployed API origin, e.g. https://findme-rau7.onrender.com
VITE_API_URL=
